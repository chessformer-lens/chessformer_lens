# Everything the app needs, from a clone: `pip install -r requirements.txt`.
#
# The first line installs the Maia-3 model code straight from GitHub, so there is
# nothing else to clone. It lives here rather than in pyproject.toml because it
# is not on PyPI, and PyPI rejects direct git URLs in dependency metadata.
maia3 @ git+https://github.com/CSSLab/maia3

# The package itself, editable, with both frontends (pywebview + matplotlib).
# This is what puts the `chessformer-lens` command on your PATH; torch, numpy,
# python-chess, huggingface-hub and ipython come along as its dependencies.
-e .[all]
